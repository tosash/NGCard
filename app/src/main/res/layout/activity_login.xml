<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:fitsSystemWindows="true"
    android:paddingLeft="70dp"
    android:paddingRight="70dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/layout_emailEditText"
        android:layout_centerHorizontal="false"
        android:layout_marginBottom="40dp"
        android:gravity="center_horizontal"
        android:text="Для регистрации или входа укажите свой номер телефона, а затем введите код полученный по СМС."
        android:textSize="16sp" />


    <android.support.design.widget.TextInputLayout
        android:id="@+id/layout_emailEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        app:hintTextAppearance="@style/TextAppearance.AppCompat">

        <EditText
            android:id="@+id/emailEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/white"
            android:hint="Номер телефона"
            android:inputType="phone"
            android:maxLines="1"
            android:selectAllOnFocus="true" />

    </android.support.design.widget.TextInputLayout>

    <Button
        android:id="@+id/remember"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/layout_emailEditText"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="30dp"
        android:background="@drawable/rounded_green"
        android:elevation="2dp"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:text="Получить СМС"
        android:textColor="@android:color/white"
        android:textSize="18sp" />


</RelativeLayout>
